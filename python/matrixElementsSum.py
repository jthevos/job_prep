def matrixElementsSum(matrix):
    """
    This is a 2D array. Thus, the rows are how many entries, and
    the columns is how long each entry is. 
    """
    rows = len(matrix)    #
    cols = len(matrix[0]) # getting any row length will give us the columns
